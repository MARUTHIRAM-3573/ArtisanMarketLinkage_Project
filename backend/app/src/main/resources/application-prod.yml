spring:
  jpa:
    show-sql: false

logging:
  level:
    root: WARN
    com.artisanplatform: INFO

management:
  endpoint:
    health:
      show-details: never

# All secrets (DB password, JWT signing key, AI/payment API keys) MUST be
# injected as environment variables by the deployment platform — never
# committed here. No Vault is used for MVP (ADR-10); see
# docs/architecture/08_SECURITY_AND_VAULT.md Part B.
